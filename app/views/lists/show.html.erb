<div class= "container" >
  <div class= "row" >
    <div class= "col-md-6" >
      <%=  render 'list'  %>
      <p> <%= link_to 'Edit', edit_list_path(@list) %> </p>
      <p> <%= link_to 'Back to list', lists_path %> </p>
      <%= link_to 'Delete', @restaurant, method: :delete, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %> </small>
    </div>
  </div>
</div>
